// Find the largest number

let a=20,b=20,c=30;

// If a is greater then b and a is greater than c, so its largest number
if(a>b && a>c){
    console.log("a is the largest number")
}else if(b>a && b>c)
{
    console.log("b is the largest number")
}else
{
    console.log("c is the largest number")
}